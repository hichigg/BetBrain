# ── Required API Keys ──────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
ODDS_API_KEY=
BALLDONTLIE_API_KEY=

# ── Server Config ──────────────────────────────────
PORT=3001
NODE_ENV=development

# ── CORS (comma-separated origins) ─────────────────
CORS_ORIGIN=http://localhost:5173

# ── Database Path (default: ./betbrain.db) ─────────
# DATABASE_PATH=/data/betbrain.db

# ── Rate Limiting (requests per minute, prod only) ─
# RATE_LIMIT_GENERAL=100
# RATE_LIMIT_ANALYSIS=10
